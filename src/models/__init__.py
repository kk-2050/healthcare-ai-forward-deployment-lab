# File Name: __init__.py
# Purpose: Marks src/models as the package containing Phase 1 Pydantic data models.
# Creation Date: 2026-09-14
# Author: K.Kashiwagi
#
# Module Explanation:
# This package holds the Pydantic models that define the shape of data
# moving through the project: the case itself (case.py), completeness
# rule inputs/outputs (rules.py), AI task/routing models (ai.py), and
# the FastAPI request/response contract (api.py).
