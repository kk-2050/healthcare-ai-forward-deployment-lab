# File Name: __init__.py
# Purpose: Marks src/workflow as the package containing the Phase 1 LangGraph workflow.
# Creation Date: 2026-09-14
# Author: K.Kashiwagi
#
# Module Explanation:
# This package wires the deterministic rules and the AI service together
# into one explicit, inspectable LangGraph state machine: the shared
# state shape (state.py), the individual processing steps (nodes.py),
# and the graph that connects them with deterministic routing (graph.py).
